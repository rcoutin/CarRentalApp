<h1>Reserve a Car</h1>
<%= form_with scope: :reservation, url: reservations_path, local: true do |form| %>

  <% if !is_customer? %>
    <p>
      <%= form.label :customer_id %>:
      <%= form.text_field :customer_id, value: session[:current_user] %>
    </p>
  <% else %>
    <%= form.hidden_field :customer_id, value: session[:current_user] %>
  <% end %>

  <p>
    <%= form.hidden_field :car_id, value: params[:car_id] %>
    <%= form.label :car_id %>:
    <%= params[:car_manufacturer] %>
    <%= params[:car_model] %>
  </p>

  <p>
    <%= form.label :from_time %>:
    <%= datetime_local_field(:reservation, :from_time) %>
  </p>


  <p>
    <%= form.label :to_time %>:
    <%= datetime_local_field(:reservation, :to_time) %>
  </p>

  <p>
    <%= form.submit "Reserve", :class => "btn btn-success"%>
  </p>
<% end %>
