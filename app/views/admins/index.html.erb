<h1>Listing admins</h1>

<table>
  <tr>
  <th>First Name</th>
    <th>Last Name</th>
  </tr>

  <% @admins.each do |admin| %>
    <tr>
      <td><%= admin.first_name %></td>
      <td><%= admin.last_name %></td>
      <td><%= link_to 'Show', admin_path(admin) , :class => "btn btn-success"%></td>
    </tr>
  <% end %>
</table>
